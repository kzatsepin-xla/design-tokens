# Design Tokens

Автоматическая конвертация дизайн-токенов из Figma в CSS и SCSS переменные с помощью Style Dictionary.

## 📁 Структура проекта

```
design-tokens/
├── tokens/
│   └── tokens.json          # Исходные токены из Figma (W3C DTCG формат)
├── config/
│   └── style-dictionary.config.js  # Конфигурация Style Dictionary
├── build/
│   ├── css/
│   │   └── variables.css    # Сгенерированные CSS переменные
│   └── scss/
│       └── variables.scss   # Сгенерированные SCSS переменные
└── package.json              # Зависимости проекта
```

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

Это установит Style Dictionary - инструмент для конвертации токенов.

### Шаг 2: Генерация файлов

Запустите команду для генерации CSS и SCSS файлов:

```bash
npm run build
```

После выполнения команды в папке `build/` появятся:
- `build/css/variables.css` - CSS переменные
- `build/scss/variables.scss` - SCSS переменные

### Шаг 3: Использование в проекте

**CSS:**
```css
@import './build/css/variables.css';

.my-button {
  background-color: var(--color-brand-primary-default);
  padding: var(--spacing-md);
}
```

**SCSS:**
```scss
@import './build/scss/variables.scss';

.my-button {
  background-color: $color-brand-primary-default;
  padding: $spacing-md;
}
```

## 📝 Формат токенов

Исходный файл `tokens/tokens.json` должен быть в формате W3C DTCG:

```json
{
  "Colors/Mode 1": {
    "brand": {
      "primary": {
        "default": {
          "$type": "color",
          "$value": "#ff6b35"
        }
      }
    }
  },
  "Spacing/Mode 1": {
    "md": {
      "$type": "number",
      "$value": 16
    }
  }
}
```

## 🔄 Автоматическая генерация

### Локальная разработка

Для автоматической генерации при изменении токенов используйте режим наблюдения:

```bash
npm run build:watch
```

Эта команда будет автоматически перегенерировать файлы при каждом изменении `tokens/tokens.json`.

### GitHub Actions (CI/CD)

Проект настроен с автоматической конвертацией через GitHub Actions:

- **Триггер**: При каждом push в ветку `main` с изменениями в папке `tokens/`
- **Действия**:
  1. Установка Node.js и зависимостей
  2. Запуск конвертации токенов
  3. Автоматический коммит и push сгенерированных файлов в `build/`

**Как это работает:**
1. Вы обновляете `tokens/tokens.json` в Figma и пушите изменения в `main`
2. GitHub Action автоматически запускается
3. Токены конвертируются в CSS и SCSS
4. Результаты автоматически коммитятся обратно в репозиторий

Файл workflow: `.github/workflows/build-tokens.yml`

## 📤 Формат вывода

### CSS переменные
```css
:root {
  --color-brand-primary-default: #ff6b35;
  --spacing-md: 16px;
}
```

### SCSS переменные
```scss
$color-brand-primary-default: #ff6b35;
$spacing-md: 16px;
```

## 🛠️ Настройка

Все настройки конвертации находятся в файле `config/style-dictionary.config.js`.

Вы можете:
- Изменить формат имен переменных
- Добавить новые форматы вывода (LESS, Stylus и т.д.)
- Настроить трансформации значений
- Добавить префиксы к переменным

## 📚 Дополнительная информация

- [Style Dictionary документация](https://amzn.github.io/style-dictionary/)
- [W3C Design Tokens формат](https://tr.designtokens.org/format/)
